---
import type { Tool } from '../data/tools';

interface Props {
  tool: Tool;
  index?: number;
}

const { tool, index = 0 } = Astro.props;
const href = tool.page ?? tool.url;

const kindLabel = {
  web: 'Web app',
  cli: 'CLI',
  both: 'Web app + CLI',
}[tool.kind];
---

<article class="mp-card tool-card mp-animate-in" style={`animation-delay: ${index * 80}ms`}>
  <div class="tool-card-header">
    <span class="tool-card-name">{tool.name}</span>
    <span class={`tool-card-pill pill-kind-${tool.kind}`}>{kindLabel}</span>
    <span class="tool-card-pill">{tool.category}</span>
  </div>

  <div class="tool-card-tagline">{tool.tagline}</div>

  <p class="tool-card-desc">{tool.description}</p>

  <div class="tool-card-links">
    {href ? (
      <>
        <a href={href} target={tool.page ? undefined : '_blank'} rel={tool.page ? undefined : 'noopener'}>
          {tool.page ? 'Learn more →' : 'Open tool →'}
        </a>
        {tool.github && (
          <a href={tool.github} target="_blank" rel="noopener">GitHub</a>
        )}
      </>
    ) : tool.github && (
      <a href={tool.github} target="_blank" rel="noopener">GitHub →</a>
    )}
    {tool.manuscript && (
      <a href={tool.manuscript} target="_blank" rel="noopener">Paper</a>
    )}
  </div>
</article>
